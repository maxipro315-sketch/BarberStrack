rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /barbers/{userId}/app/{documentId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}